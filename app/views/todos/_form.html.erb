<%= form_for @todo do |f| %>

<p>
  <%= f.label :name %><br />
  <%= f.text_field :name %>
</p>

<p>
  <%= f.label :description %><br />
  <%= f.text_area :description %>
</p>


<%= f.submit ( f.object.new_record? ? "Create Todo" : "Update Todo") %>

<% end %>